# Versioning

Formal releases use Semantic Versioning:

- MAJOR — incompatible product/API change;
- MINOR — backward-compatible feature;
- PATCH — backward-compatible fix.

During incubation, use `0.x.y`. Release candidates may use `-rc.N`.
